require('glow').setup({
  style = "dark",
  pager = false,
  width = 120,
  height = 100,
  width_ratio = 0.5, -- maximum width of the Glow window compared to the nvim window size (overrides `width`)
  height_ratio = 0.5
})
